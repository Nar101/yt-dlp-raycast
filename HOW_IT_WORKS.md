# Media Saver 是怎么工作的

这份文档给想了解细节的用户和开发者看。日常使用不需要读懂这些内容。

## 一句话说明

Media Saver 是一个 Raycast 界面。它负责接收链接、创建任务、展示状态和打开文件；真正的媒体获取和文件处理在本机的后台任务中完成。

当前底层提供 1,700+ 个站点/服务解析器，因此它不是只针对某一个平台的下载器。具体支持情况会随网站规则和当前版本变化。

## 用户看到的流程

1. 在 Raycast 中打开 `Save Media`。
2. 粘贴一个或多个媒体链接。
3. 选择保存为视频或音频，也可以选择字幕。
4. Media Saver 先检查这台 Mac 是否准备好。
5. 任务进入 `Download Queue`，可以看到排队、进度、完成或失败。
6. 即使关闭 Raycast，后台任务仍会继续；之后重新打开 `Download Queue` 就能查看结果。

## 为什么需要额外组件

不同网站的媒体地址、格式和登录方式差异很大。Media Saver 使用两个成熟的本地组件处理这些差异：`yt-dlp` 负责获取媒体，`ffmpeg` 负责视频合并、音频提取等文件处理工作。

它们都运行在用户自己的 Mac 上。Media Saver 不把链接、登录信息或下载内容上传到云端。

首次使用前，`Download Setup` 会检查：

- 媒体获取组件是否存在并可运行；
- 视频处理组件是否存在并可运行；
- 保存目录是否可写；
- 磁盘是否有足够空间。

如果这两个组件缺少，扩展不会静默替用户安装，而是给出明确步骤。已经安装 Homebrew 的用户通常可以在 Terminal 中运行：

```bash
brew install yt-dlp ffmpeg
```

这样用户知道电脑发生了什么，也不会在没有确认的情况下执行系统安装。

## 为什么关闭 Raycast 后任务不会消失

Raycast 界面和后台任务是分开的：

- Raycast 只负责操作界面；
- 每个下载任务会启动独立的后台进程；
- 任务状态会写入本机的应用支持目录；
- `Download Queue` 每秒读取状态并刷新进度。

因此关闭 Raycast 不等于取消下载。任务列表重新打开后，会继续显示任务当前状态。

## 任务保存在哪里

- 新媒体文件：`~/Downloads/Media Saver/`
- 任务状态：`~/Library/Application Support/yt-dlp-raycast/`

旧版本的 `~/Downloads/yt-dlp/` 文件不会自动移动或删除。

## 失败时怎么办

任务失败后不会直接消失。列表会显示大致原因，例如：

- 链接不完整或网站不支持；
- 内容已经删除、设为私密或受地区限制；
- 网站要求登录；
- 网络、代理或站点限流；
- 保存目录没有权限或磁盘空间不足；
- 视频处理组件异常。

先打开任务详情查看建议，再使用 `Retry Download` 重试。重试会新建一次尝试，原记录仍然保留。

## 隐私边界

- 链接、任务状态、登录信息和媒体文件只在本机处理；
- 浏览器登录只在用户主动选择后使用；
- 仓库和公开作品不包含本机任务、媒体或账号凭证；
- 只保存你有权保存或处理的内容，并遵守来源网站规则。

## 开发者备注

底层媒体获取组件的路径会从常见的 Homebrew 位置和当前 `PATH` 自动探测，而不是写死某一种 Mac 架构。后台任务状态使用 JSON 文件保存，便于在 Raycast 重启后恢复可观察性。扩展本身只负责界面、状态和任务控制，不把这些组件打包进公开扩展。
